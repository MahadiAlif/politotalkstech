---
const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container">
    <div class="footer-content">
      <p class="copyright">
        © {currentYear} PolitoTalksTech. A student publication at Politecnico di Torino.
      </p>
      <nav class="footer-nav">
        <a href="mailto:contact@politotalkstech.edu">Contact</a>
        <a href="https://github.com/politotalkstech" target="_blank" rel="noopener noreferrer">GitHub</a>
        <a href="/submit">Submit</a>
      </nav>
    </div>
  </div>
</footer>

<style>
.site-footer {
  border-top: 1px solid var(--color-border);
  padding: var(--space-8) 0;
  margin-top: var(--space-16);
}

.footer-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.copyright {
  font-size: var(--text-sm);
  color: var(--color-text-tertiary);
  margin: 0;
}

.footer-nav {
  display: flex;
  gap: var(--space-4);
}

.footer-nav a {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  text-decoration: none;
  transition: color var(--transition-fast);
}

.footer-nav a:hover {
  color: var(--color-text-primary);
}

@media (max-width: 768px) {
  .footer-content {
    flex-direction: column;
    gap: var(--space-4);
    align-items: flex-start;
  }
}
</style>
